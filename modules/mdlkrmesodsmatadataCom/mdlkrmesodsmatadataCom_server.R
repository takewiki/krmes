# 
# mdlDemoServer::buttonServer(input = input,output = output,session = session,dms_token = dms_token)
# 
# #完成日期组件注册
# mdlDemoServer::dateServer(input = input,output = output,session = session,dms_token = dms_token)


#jhhrv
# mdljhhrvServer::HrvServer(input = input,output = output,session = session,dms_token = dms_token)

# krmes
mdlkrmesodsmatadataServer::krmesstockodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsmatadataServer::krmesrouteodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsmatadataServer::krmesproductionwarehousinodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsmatadataServer::krmesproductionreturnodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsmatadataServer::krmesproductionrequisitionodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsmatadataServer::krmesproductionorderodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsmatadataServer::krmesproductionfeedodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsmatadataServer::krmesprocesstransferodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsmatadataServer::krmesprocessreportodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsmatadataServer::krmesprocessplanningodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsmatadataServer::krmesprocessodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsmatadataServer::krmesmaterialodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsmatadataServer::krmesflexvaluesodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsmatadataServer::krmesempodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsmatadataServer::krmesdeptodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsmatadataServer::krmesbomodsmatadataServer(input = input,output = output,session = session,dms_token = dms_token)



  

