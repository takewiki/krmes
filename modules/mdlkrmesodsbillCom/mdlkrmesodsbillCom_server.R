# 
# mdlDemoServer::buttonServer(input = input,output = output,session = session,dms_token = dms_token)
# 
# #完成日期组件注册
# mdlDemoServer::dateServer(input = input,output = output,session = session,dms_token = dms_token)


#jhhrv
# mdljhhrvServer::HrvServer(input = input,output = output,session = session,dms_token = dms_token)

# krmes
# mdlkrmesodsbillServer::krmesstockodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsbillServer::krmesrouteodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsbillServer::krmesproductionwarehousinodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsbillServer::krmesproductionreturnodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsbillServer::krmesproductionrequisitionodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsbillServer::krmesproductionorderodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsbillServer::krmesproductionfeedodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsbillServer::krmesprocesstransferodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsbillServer::krmesprocessreportodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmesodsbillServer::krmesprocessplanningodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsbillServer::krmesprocessodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsbillServer::krmesmaterialodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsbillServer::krmesflexvaluesodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsbillServer::krmesempodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsbillServer::krmesdeptodsbillServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmesodsbillServer::krmesbomodsbillServer(input = input,output = output,session = session,dms_token = dms_token)



  

